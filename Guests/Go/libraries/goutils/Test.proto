syntax = "proto3";

// metaffi_target_language: "go"

// metaffi_function_path: "module=github.com/Masterminds/goutils"
service Test
{
	rpc initials (initials_params) returns (initials_return); // returns initials. metaffi_function_path: "function=Initials"
}

message initials_params
{
	string input = 1;
}
message initials_return
{
	string res = 1;
}
